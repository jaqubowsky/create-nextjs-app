pre-commit:
  parallel: true
  commands:
    lint:
      glob: "*.{js,jsx,ts,tsx}"
      run: npm run lint -- --fix {staged_files}
      stage_fixed: true
    typecheck:
      run: npm run typecheck

commit-msg:
  scripts:
    "commitlint.sh":
      runner: bash
